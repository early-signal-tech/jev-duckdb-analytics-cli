[project]
name = "jev-tests"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "click>=8.5.0",
    "duckdb>=1.5.5",
    "python-dotenv>=1.2.3",
    "typesafe-sdk>=0.7.1",
]
